name: Build and Push Firefox Docker Image

# 仅手动触发，并允许自定义镜像标签
on:
  workflow_dispatch:
    inputs:
      image-tag:
        description: '镜像标签 (例如: latest, v1.0)'
        required: true
        default: 'latest'
        type: string

# 在此定义全局环境变量，方便集中修改
env:
  REGISTRY: ghcr.io
  # 🔧 【核心修改点】在这里设置你想要的镜像名！
  # 格式为：你的用户名/镜像名
  # 例如，如果你想命名为 my-firefox，就改为：${{ github.repository_owner }}/my-firefox
  # 如果你想直接命名为 firefox，就改为：${{ github.repository_owner }}/firefox
  IMAGE_NAME: ${{ github.repository_owner }}/dbfirefox

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # 使用上面定义的变量，标签支持手动输入
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.event.inputs.image-tag }}
          # 🛠️ 【关键修复】移除了导致错误的 `cache-from` 和 `cache-to` 配置
          # 如果未来需要缓存，可参考官方文档配置兼容模式
