name: 构建 Docker 镜像

on:
  workflow_dispatch:
    inputs:
      custom_image_name:
        description: '自定义的镜像名称（例如：myapp/firefox-novnc，留空则默认为firefox）'
        required: false  # 由于有默认值，可以设为非必需
        default: 'firefox'  # 关键修改：默认镜像名设为 firefox

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录到 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并推送
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ github.event.inputs.custom_image_name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 输出镜像信息
        run: |
          echo "✅ 镜像构建并推送成功！"
          echo "📦 镜像地址：ghcr.io/${{ github.repository_owner }}/${{ github.event.inputs.custom_image_name }}:latest"
          echo "📥 拉取命令：docker pull ghcr.io/${{ github.repository_owner }}/${{ github.event.inputs.custom_image_name }}:latest"
